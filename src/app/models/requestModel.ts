export default interface RequestModel {
    firstName: string;
    lastName: string;
    id?: string;
    birthDay: string;
    university: string;
    email: string;
    password: string;
    caUrl?: string;
}